<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= q.title %> - NutriBaba</title>
    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/components.css">
    <link rel="stylesheet" href="/css/animations.css">
    <link rel="stylesheet" href="/css/pages.css">
    <link rel="stylesheet" href="/css/mobile.css">
</head>
<body>
    <div class="app-container">
        <!-- Sidebar -->
        <% if (user) { %>
        <aside class="sidebar" id="sidebar">
            <div class="brand">
                <h1>NutriBaba</h1>
            </div>
            
            <% if (user.role === 'user') { %>
                <nav class="nav-group">
                    <h2 class="nav-title">User Menu</h2>
                    <div class="nav-items">
                        <a href="/user/profile" class="<%= path === '/user/profile' ? 'active' : '' %>">
                            <svg class="nav-icon"><use href="/svg/icons.svg#icon-profile"/></svg>
                            My Profile
                        </a>
                        <a href="/user/post-query" class="<%= path === '/user/post-query' ? 'active' : '' %>">
                            <svg class="nav-icon"><use href="/svg/icons.svg#icon-query"/></svg>
                            Post Query
                        </a>
                        <a href="/user/queries" class="<%= path === '/user/queries' ? 'active' : '' %>">
                            <svg class="nav-icon"><use href="/svg/icons.svg#icon-solution"/></svg>
                            My Queries
                        </a>
                        <a href="/user/tips" class="<%= path === '/user/tips' ? 'active' : '' %>">
                            <svg class="nav-icon"><use href="/svg/icons.svg#icon-tips"/></svg>
                            Health Tips
                        </a>
                        <a href="/user/solutions" class="<%= path === '/user/solutions' ? 'active' : '' %>">
                            <svg class="nav-icon"><use href="/svg/icons.svg#icon-solution"/></svg>
                            View Solutions
                        </a>
                    </div>
                </nav>
            <% } else if (user.role === 'expert') { %>
                <nav class="nav-group">
                    <h2 class="nav-title">Expert Menu</h2>
                    <div class="nav-items">
                        <a href="/expert" class="<%= path === '/expert' ? 'active' : '' %>">
                            <svg class="nav-icon"><use href="/svg/icons.svg#icon-query"/></svg>
                            View Queries
                        </a>
                        <a href="/expert/solutions" class="<%= path === '/expert/solutions' ? 'active' : '' %>">
                            <svg class="nav-icon"><use href="/svg/icons.svg#icon-solution"/></svg>
                            My Solutions
                        </a>
                        <a href="/expert/feedback" class="<%= path === '/expert/feedback' ? 'active' : '' %>">
                            <svg class="nav-icon"><use href="/svg/icons.svg#icon-feedback"/></svg>
                            View Feedback
                        </a>
                        <a href="/expert/tips" class="<%= path === '/expert/tips' ? 'active' : '' %>">
                            <svg class="nav-icon"><use href="/svg/icons.svg#icon-tips"/></svg>
                            Health Tips
                        </a>
                    </div>
                </nav>
            <% } %>
        </aside>
        <% } %>

        <div class="main-content <%= user ? '' : 'full' %>" id="main">
            <!-- Header -->
            <header class="site-header">
                <div class="header-container">
                    <% if (user) { %>
                    <button id="sidebar-toggle" class="menu-toggle">
                        <svg width="24" height="24" fill="none" stroke="currentColor">
                            <path d="M4 6h16M4 12h16M4 18h16" stroke-width="2" stroke-linecap="round"/>
                        </svg>
                    </button>
                    <% } else { %>
                    <a href="/" class="brand-logo">
                        <h1>NutriBaba</h1>
                    </a>
                    <% } %>
                    
                    <div class="header-right">
                        <% if (user) { %>
                            <span class="user-name"><%= user.name %></span>
                            <a href="/auth/logout" class="btn btn-outline">Logout</a>
                        <% } else { %>
                            <a href="/auth/login" class="btn btn-outline">Login</a>
                            <a href="/auth/register" class="btn btn-primary">Register</a>
                        <% } %>
                    </div>
                </div>
            </header>

            <!-- Main Content -->
            <main class="content">
                <div class="container">
                    <div class="query-detail fade-in">
                        <div class="page-header">
                            <div class="header-content">
                                <h2 class="page-title"><%= q.title %></h2>
                                <div class="query-meta">
                                    <div class="meta-item">
                                        <svg class="icon" width="16" height="16"><use href="/svg/icons.svg#icon-profile"/></svg>
                                        <span><%= q.postedBy ? q.postedBy.name : 'Unknown' %></span>
                                    </div>
                                    <div class="meta-item">
                                        <svg class="icon" width="16" height="16"><use href="/svg/icons.svg#icon-time"/></svg>
                                        <time datetime="<%= q.createdAt %>"><%= new Date(q.createdAt).toLocaleString() %></time>
                                    </div>
                                    <div class="query-badges">
                                        <% if (q.solutions && q.solutions.length > 0) { %>
                                            <span class="badge badge-success">Answered</span>
                                        <% } else { %>
                                            <span class="badge badge-warning">Pending</span>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                            <div class="header-actions">
                                <button class="btn btn-outline" id="shareBtn">
                                    <svg class="icon" width="16" height="16"><use href="/svg/icons.svg#icon-share"/></svg>
                                    Share
                                </button>
                            </div>
                        </div>

                        <div class="card query-content">
                            <p><%= q.description %></p>
                        </div>

                        <section class="solutions-section">
                            <h3 class="section-title">Expert Solutions</h3>
                            <% if (q.solutions && q.solutions.length > 0) { %>
                                <% q.solutions.forEach((solution, index) => { %>
                                    <div class="solution slide-in">
                                        <div class="solution-header">
                                            <div class="expert-info">
                                                <div class="expert-avatar"><%= solution.expert ? solution.expert.name.charAt(0) : 'E' %></div>
                                                <div>
                                                    <strong class="expert-name"><%= solution.expert ? solution.expert.name : 'Expert' %></strong>
                                                    <div class="solution-meta">
                                                        <time datetime="<%= solution.createdAt %>"><%= new Date(solution.createdAt).toLocaleString() %></time>
                                                    </div>
                                                </div>
                                            </div>
                                            <button class="btn btn-ghost solution-actions-toggle">
                                                <svg class="icon" width="16" height="16"><use href="/svg/icons.svg#icon-more"/></svg>
                                            </button>
                                        </div>
                                        <div class="solution-content">
                                            <%= solution.content %>
                                        </div>
                                        <div class="solution-footer">
                                            <button class="btn btn-ghost like-btn">
                                                <svg class="icon" width="16" height="16"><use href="/svg/icons.svg#icon-heart"/></svg>
                                                Helpful
                                            </button>
                                            <% if (user) { %>
                                            <button class="btn btn-outline toggle-feedback-btn" data-target="feedback-form-<%= index %>">
                                                <svg class="icon" width="16" height="16"><use href="/svg/icons.svg#icon-feedback"/></svg>
                                                Give Feedback
                                            </button>
                                            <% } %>
                                        </div>
                                        
                                        <!-- Individual Feedback Form for Each Solution -->
                                        <% if (user) { %>
                                        <div class="feedback-form-container" id="feedback-form-<%= index %>" style="display: none;">
                                            <hr>
                                            <h4>Give Feedback to <%= solution.expert ? solution.expert.name : 'Expert' %></h4>
                                            <form action="/user/feedback" method="post" class="feedback-form">
                                                <input type="hidden" name="queryId" value="<%= q._id %>">
                                                <input type="hidden" name="expertId" value="<%= solution.expert ? solution.expert._id : '' %>">
                                                
                                                <div class="form-group">
                                                    <label class="form-label">Your Message</label>
                                                    <textarea name="message" class="form-control" required 
                                                        placeholder="Share your thoughts about this solution..."></textarea>
                                                </div>
                                                
                                                <div class="form-group">
                                                    <label class="form-label">Rating</label>
                                                    <div class="rating" id="ratingStars-<%= index %>">
                                                        <input type="hidden" name="rating" id="ratingInput-<%= index %>">
                                                        <% for (let i = 1; i <= 5; i++) { %>
                                                            <button type="button" class="star-btn" data-rating="<%= i %>" data-form="<%= index %>">
                                                                <svg class="star" width="20" height="20">
                                                                    <use href="/svg/icons.svg#icon-star"/>
                                                                </svg>
                                                            </button>
                                                        <% } %>
                                                    </div>
                                                    <small class="rating-display" id="rating-display-<%= index %>">Click stars to rate (1-5)</small>
                                                </div>

                                                <div class="form-actions">
                                                    <button type="button" class="btn btn-ghost cancel-feedback-btn" data-target="feedback-form-<%= index %>">
                                                        Cancel
                                                    </button>
                                                    <button type="submit" class="btn btn-primary">
                                                        Send Feedback
                                                    </button>
                                                </div>
                                            </form>
                                        </div>
                                        <% } %>
                                    </div>
                                <% }) %>
                            <% } else { %>
                                <div class="empty-state">No solutions yet. Experts will respond soon.</div>
                            <% } %>
                        </section>
                    </div>
                </div>
            </main>
        </div>
    </div>
    
    <script src="/js/main.js"></script>
</body>
</html>
